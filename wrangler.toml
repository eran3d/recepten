name = "recepten"
main = "src/worker.js"
compatibility_date = "2026-07-02"

# Static assets uit public/ worden vóór de Worker geserveerd;
# alleen niet-asset requests (zoals POST /api/submit) bereiken worker.js.
[assets]
directory = "./public"
